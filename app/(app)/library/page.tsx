// Day 1 placeholder so we can verify auth end-to-end.
// Agent B owns the real implementation per SPEC.md Section 7.

export default function LibraryPage() {
  return (
    <main className="flex min-h-svh items-center justify-center p-8">
      <p className="text-sm text-muted-foreground">Library — authenticated</p>
    </main>
  );
}
